extends ActionStep

# Raising a hand at somebody. It does nothing to the world yet — no standing
# moves, no rung changes — because Gate 2 has not been cut and authoring an
# effect here would be authoring it against a guess.
#
# What it DOES do is show, which is the whole of what this scene is for: the
# clip is authored below like every other step's, so person.gd plays it through
# the exact path that already exists and nothing new decides which animation a
# verb means.
